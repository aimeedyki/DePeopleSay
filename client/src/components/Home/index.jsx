import React from 'react';

// import HomeContainer from './HomeContainer';
import HomeComponent from './HomeComponent';

const Home = props => (
  // <HomeContainer {...props} component={HomeComponent} />
  <HomeComponent />
);

export default Home;
